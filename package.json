{
  "scripts": {
    "prepare": "husky && husky install"
  },
  "devDependencies": {
    "git-format-staged": "^3.1.1",
    "husky": "^9.1.5",
    "lint-staged": "^15.2.9",
    "prettier": "^3.3.3"
  },
  "lint-staged": {
    "*.{js,css,md}": "prettier --write"
  }
}
